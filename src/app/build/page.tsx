import { BuilderClient } from "@/components/builder/BuilderClient";

export const metadata = {
  title: "Build a skill · Team Skill Builder",
};

export default function BuildPage() {
  return <BuilderClient />;
}
